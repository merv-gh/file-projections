# build outputs
bin/
file-projections
.projections/
tmp/
# Joern artifacts (regenerable)
workspace/
.projections/.cpg/
.projections/.joern-*.jsonl

# OS noise
.DS_Store
*.bin
.gradle/
.code-review-graph
.lock
build/

# editor / tooling
.vscode/
.claude/
__pycache__/
*.pyc

# local working notes (not part of the repo)
handoff.md

# benchmark run artifacts (regenerable; keep results.json + report.png + READMEs)
tools/benchmark/harness/*.jsonl
tools/benchmark/harness/mcp-*.json
todo.md

# proj-mcp per-fixture config (generated by the bench MCP at run time)
fixtures/*/fp.config.json
review.md